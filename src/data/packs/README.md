Content packs currently live in TypeScript modules under `src/data/practice`
and `src/toeic/data/practice`.

For Wave 4 CMS work, these packs can be exported as JSON while keeping the
same `UnitPractice` shape used by the exercise engine.
